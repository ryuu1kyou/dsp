kesyou
motion = optimal(i,3);
motion = OpenIndexValue(motion, State);

motion_size = motion(1,15);
nextFlex = motion(1,1:motion_size);
nextFlex_idx = find(nextFlex==2);
nextFlex(nextFlex_idx) = -1;
vssUpDown = motion(1,16);
 
sei
motion_data = [ nextFlex(mind, :) zeros(1,State.motionEnd - State.motionInd + 1 - length(nextFlex(mind, :)))];

motion_data_idx = find(motion_data==-1);
motion_data(motion_data_idx) = 2;

motion_data(end-1) = length(nextFlex(mind, :));
motion_data(end) = vssUpDownFlag;


  if findFlag == 1 
          openList(targetInd, State.motionInd:State.motionEnd) = motion_data;
  elseif findFlag == 2 
          closeList(targetInd, State.motionInd:State.motionEnd) = motion_data;
      end
  else
      .... TargetNode CalculateIndexValue(nextNode, State) motion_data]];
  end

.motionInd = State.childIndexInd+1;
.motionEnd = State.childIndexInd+State.node_MAX;
.tableParent_Motion = 3;
.tableChild_Motion = 6;
